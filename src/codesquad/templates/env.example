# Copy to .env and fill in only the providers your codesquad.yaml uses.
# Secrets/endpoints only — model choices live in codesquad.yaml.
OPENAI_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=

# Ollama needs no key; uncomment only if it runs elsewhere than localhost:11434.
# OLLAMA_API_BASE=http://localhost:11434
